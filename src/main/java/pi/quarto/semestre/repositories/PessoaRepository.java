package pi.quarto.semestre.repositories;
/*
 * import java.util.Optional;
 * 
 * import org.springframework.data.jpa.repository.Query; import
 * org.springframework.data.repository.CrudRepository; import
 * org.springframework.stereotype.Repository; import
 * org.springframework.transaction.annotation.Transactional;
 * 
 * import antlr.collections.List; import pi.quarto.semestre.models.Pessoa;
 * 
 * @Repository
 * 
 * @Transactional public interface PessoaRepository extends
 * CrudRepository<Pessoa, Long> {
 * 
 * @Query("select p from Pessoa p where p.email = :email") public Pessoa
 * findByEmail(String email);
 * 
 * 
 * @Query(value="select * from Pessoa where email = :email and senha= :senha",
 * nativeQuery = true) public Pessoa Login(String email, String senha);
 * 
 * @Query(value="select * from Pessoa where email = :email", nativeQuery = true)
 * public Pessoa Login2(String email);
 * 
 * @Query(value="select * from Pessoa where id = :id", nativeQuery = true)
 * public Pessoa findUsuarioById(long id); }
 */